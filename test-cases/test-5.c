#include <stdio.h>
int main(int argc, char const *argv[])
{
	int i,j,a,b,c,d,x,num1,num2;
	scanf("%d",&a);
	scanf("%d",&b);
	scanf("%d",&c);
	scanf("%d",&d);

	num1=a;
	num2=b;
	for(i=0;i<10;i++)
	{
		for(j=0;j<10;j++)
		{
			a=(c/b)+(b*i);
			x=b*i+num1
			if(i==j)
				d=a*c;
			a=a-c;
		}
		num2=b+c;
	}
	return 0;
}